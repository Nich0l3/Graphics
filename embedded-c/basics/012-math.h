
#ifndef MATH_H_ // Include Gurads - prevent the inclusion of header files 
		// multiple times in the source file.
		// Written using pre-processor directives.
#define MATH_H_

int add_f(int, int);
int sub_f(int, int);
long long int mul_f(int, int);
float div_f(int, int);


#endif
